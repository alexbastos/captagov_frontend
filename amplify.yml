version: 1
applications:
  - appRoot: apps/web
    frontend:
      phases:
        preBuild:
          commands:
            - npm install -g pnpm
            - pnpm --dir ../.. install
        build:
          commands:
            - pnpm --dir ../.. run build
            - rm -rf node_modules
            - cp -rL .next/standalone/apps/web/node_modules .
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - .next/cache/**/*
          - ../../node_modules/**/*
